package com.example.demo.controller;nnimport com.example.demo.entity.License;nimport com.example.demo.service.LicenseService;nimport org.springframework.beans.factory.annotation.Autowired;nimport org.springframework.http.ResponseEntity;nimport org.springframework.web.bind.annotation.*;nnimport java.util.List;nn@RestControllern@RequestMapping("/api/licenses")npublic class LicenseController {n    n    @Autowiredn    private LicenseService licenseService;n    n    @GetMappingn    public ResponseEntity<List<License>> getAllLicenses() {n        List<Customer> licenses = licenseService.getAllLicenses();n        return ResponseEntity.ok(licenses);n    }n    n    @GetMapping("/{id}")n    public ResponseEntity<License> getLicenseById(@PathVariable Long id) {n        License license = licenseService.getLicenseById(id);n        if (license != null) {n            return ResponseEntity.ok(license);n        }n        return ResponseEntity.notFound().build();n    }n    n    @PostMappingn    public ResponseEntity<License> createLicense(@RequestBody License license) {n        License savedLicense = licenseService.saveLicense(license);n        return ResponseEntity.ok(savedLicense);n    }n}