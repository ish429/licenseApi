package com.example.demo.service;nnimport com.example.demo.entity.Manager;nimport com.example.demo.repository.ManagerRepository;nimport org.springframework.beans.factory.annotation.Autowired;nimport org.springframework.security.crypto.password.PasswordEncoder;nimport org.springframework.stereotype.Service;nnimport java.util.List;nn@Servicenpublic class ManagerService {n    n    @Autowiredn    private ManagerRepository managerRepository;n    n    @Autowiredn    private PasswordEncoder passwordEncoder;n    n    public List<Manager> getAllManagers() {n        return managerRepository.findAll();n    }n    n    public Manager getManagerById(Long id) {n        return managerRepository.findById(id).orElse(null);n    }n    n    public Manager saveManager(Manager manager) {n        // 비밀번호 암호화n        manager.setPassword(passwordEncoder.encode(manager.getPassword()));n        return managerRepository.save(manager);n    }n    n    public Manager updateManager(Long id, Manager manager) {n        Manager existingManager = managerRepository.findById(id).orElse(null);n        if (existingManager != null) {n            existingManager.setUsername(manager.getUsername());n            if (manager.getPassword() != null && !manager.getPassword().isEmpty()) {n                existingManager.setPassword(passwordEncoder.encode(manager.getPassword()));n            }n            existingManager.setRole(manager.getRole());n            return managerRepository.save(existingManager);n        }n        return null;n    }n    n    public boolean deleteManager(Long id) {n        if (managerRepository.existsById(id)) {n            managerRepository.deleteById(id);n            return true;n        }n        return false;n    }n}